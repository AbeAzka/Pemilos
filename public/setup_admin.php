<?php require_once __DIR__.'/../config/config.php';
$msg='';
if($_SERVER['REQUEST_METHOD']==='POST'){check_csrf();$u=trim($_POST['username']);$p=$_POST['password'];if(strlen($p)<10)$msg='Password minimal 10 karakter.';else{$s=db()->prepare("INSERT INTO admins(username,password_hash) VALUES(?,?)");$s->execute([$u,password_hash($p,PASSWORD_DEFAULT)]);$msg='Admin berhasil dibuat. Hapus file setup_admin.php sekarang.';}}
?><!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Setup Admin</title><link rel="stylesheet" href="style.css"></head><body><main><section class="card login"><h2>Setup Admin</h2><?php if($msg):?><div class="success"><?=h($msg)?></div><?php endif;?><form method="post"><input type="hidden" name="csrf" value="<?=h(csrf())?>"><label>Username</label><input name="username" required><label>Password</label><input type="password" name="password" minlength="10" required><button>Buat Admin</button></form><p class="muted">Hapus file ini setelah akun admin dibuat.</p></section></main></body></html>
